.control-field {
  border-bottom: 1px solid var(--placeholder-color);
  box-sizing: border-box;

  .red-color {
    color: var(--error-color);
  }

  &:last-child {
    border: none;
    padding-bottom: 20px;
  }
}
